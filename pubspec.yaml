name: hometask_paket
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
description: A Dart package that provides various search algorithms (Binary Search, Linear Search, Jump Search).
homepage: https://github.com/TojiddinovMuhammadabdullox/search_algorythms
repository: https://github.com/TojiddinovMuhammadabdullox/search_algorythms

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0


flutter:
  uses-material-design: true
  plugin:
    platforms:
      android:
        package: com.example.search_algorithms_flutter
        pluginClass: SearchAlgorithmsPlugin
